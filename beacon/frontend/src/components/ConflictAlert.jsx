import React from 'react';

/**
 * ConflictAlert — props: { detected: bool, details: string | null }
 */
export default function ConflictAlert({ detected, details }) {
    return null; // TODO: implement
}
